name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
    
    # Package dependencies
  - numpy
  - matplotlib
  - bitstring
  - random
  - sys
  - copy
  - networkx

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
